# Sangwoo.top 项目实施方案（自研轻量框架）

## 一、整体架构
- **方案**：自研 Python 后端 + Astro 静态前端 + Docker 部署
- **部署环境**：AWS EC2 t2.micro (54.152.184.117)
- **域名**：sangwoo.top
- **代码仓库**：GitHub (https://github.com/firedancercn/sangwoo.top.git)
- **设计原则**：极简、自动化、低资源占用，适合单核1G内存服务器

## 二、核心技术栈
- **后端框架**：FastAPI + Uvicorn（内存占用 50-100MB）
- **数据库**：SQLite（单文件，备份即拷贝）
- **后台管理**：CRUDAdmin（基于 HTMX 的低代码 Admin）+ 自研扩展（文件上传/中文界面/自定义页面）
- **前端框架**：Astro 4.x（静态生成，GitHub Actions 构建后部署）
- **样式/交互**：TailwindCSS + Alpine.js
- **访问统计**：前端埋点 JS + Cookie 用户去重 + SQLite 存储
- **自动化**：Python 脚本（cron 定时）
- **AI 服务**：sangwoozen.com/v1 API（客服/翻译/新闻重写）
- **反向代理**：Nginx（含 SSL 自动续期）
- **系统监控**：自定义健康监控脚本（CPU/内存/磁盘，超限自动重启服务）
- **备份**：SQLite + 静态文件定时备份到 S3

## 三、数据库结构（SQLite）
1. `products`：产品管理（中英双语，自动翻译，规格 JSON，多图路径）
2. `news`：新闻管理（手动 + 爬虫，AI 重写，状态控制）
3. `site_settings`：网站设置（模块化开关 JSON）
4. `about_company`：公司介绍（中英双语）
5. `contact`：联系方式（表单开关，社交媒体）
6. `visitor_submissions`：访客表单提交记录
7. `analytics`：访问统计（日期，UV，PV，页面，来源，用户代理，Cookie ID）
8. `system_log`：系统日志（错误记录，任务执行记录）
9. `admin_sessions`：后台管理会话（二次验证令牌，过期时间）

## 四、核心功能
1. **模块化开关**：后台控制各功能模块 on/off（Hero 轮播、产品对比、查找器、荣誉资质、AI 客服等）
2. **中英自动翻译**：中文发布时触发 Webhook，调用 sangwoozen API 自动填充英文字段，含重试队列和失败通知
3. **AI 客服**：右下角浮动聊天窗口，实时调用 sangwoozen API，产品知识库对接
4. **新闻爬虫 + AI 重写**：每日 08:00 抓取目标媒体，AI 深度重写去重，自动入库，含反爬策略和失败兜底
5. **访问统计**：前端埋点 JS，Cookie 用户去重，SQLite 存储，后台可视化
6. **访客资料**：联系表单收集，后台列表 + CSV 导出
7. **系统健康监控**：实时监控系统资源，超限自动重启服务或定时重置（含优雅降级）
8. **文件管理**：产品图片上传、缩略图生成、CDN 路径管理

## 五、后台管理界面（CRUDAdmin + 自研扩展）
- **基础功能**：CRUDAdmin 自动生成 CRUD 界面（产品、新闻、设置等）
- **自研文件上传**：FastAPI UploadFile 组件，支持多图上传、格式验证、缩略图生成
- **自研中文界面**：HTMX 模板，支持中英切换
- **自研自定义页面**：
  - 访问统计看板（图表展示）
  - 模块化开关控制面板
  - 系统健康监控面板
  - 备份/恢复管理
- **权限控制**：简单角色系统（管理员/只读），基于会话和二次验证

## 六、自动化脚本（/opt/sangwoo/scripts/）
- `crawler.py`：新闻爬虫（AnandTech, Tom's Hardware 等），含反爬策略
- `ai_rewrite.py`：AI 重写引擎（调用 sangwoozen API）
- `publish.py`：发布脚本（直接写入 SQLite）
- `translate_sync.py`：翻译同步（Webhook 触发，含重试队列）
- `analytics.py`：访问统计脚本（解析前端埋点数据，存入 SQLite）
- `health_monitor.py`：系统健康监控（CPU/内存/磁盘，超限自动重启）
- `backup.py`：数据库和静态文件备份到 S3
- `restore.py`：从 S3 恢复备份

## 七、前端页面路由
- `/` 或 `/en/` → 英文首页
- `/zh/` → 中文首页
- `/products`, `/products/[slug]`, `/compare`, `/finder`
- `/news`, `/news/[slug]`
- `/about`, `/contact`
- `/admin` → 后台管理界面（CRUDAdmin）
- 双语通过路由前缀或 Cookie 切换

## 八、UI 风格
- 深色主题（参考海盗船/芝奇）
- 主色调：深灰/黑 + 品牌色（待定）
- 字体：Inter + Noto Sans SC
- 动画：淡入/滑动，克制

## 九、部署架构（Docker Compose）
- `fastapi`（端口 8000，SQLite）
- `nginx`（端口 80/443，服务 Astro 静态文件 + 反代 FastAPI）
- 无独立数据库容器，SQLite 文件存储在宿主机
- 无 Umami 容器，节省内存
- 预估总内存占用：300-400MB（含爬虫脚本运行时）

## 十、Astro 构建与部署
- **构建方式**：GitHub Actions（推送代码后自动构建）
- **部署方式**：构建产物通过 SSH 同步到 EC2 服务器
- **数据流**：
  - 产品/新闻页面：构建时 fetch FastAPI API，生成静态 HTML（SEO 友好）
  - 动态内容：客户端 JS 调用 FastAPI API 实时加载（AI 客服、表单提交）
  - 内容更新后：后台触发增量构建（affected pages only）

## 十一、实施时间线（预估 10 天）
1. 服务器准备（停 WP，装 Docker/Nginx/SSL） - 0.5天
2. FastAPI 后端搭建 + 数据库模型 - 1天
3. CRUDAdmin 集成 + 自研扩展（文件上传/中文界面） - 2天
4. Astro 前端开发（页面/组件/双语/模块化） - 2天
5. AI 客服集成 - 0.5天
6. 爬虫 + AI 重写脚本 - 1天
7. 访问统计 + 健康监控脚本 - 0.5天
8. 自动化配置（翻译 webhook、cron、备份） - 0.5天
9. GitHub Actions CI/CD 配置 - 0.5天
10. 测试 + 安全加固 - 1天
11. 内容迁移（产品图/描述） - 1天
12. 缓冲时间 - 1天

## 十二、你需要提供的
1. 品牌 Logo
2. 产品图片
3. 产品规格参数清单
4. 公司介绍文案
5. 联系方式
6. 品牌主色调偏好
7. 产品型号列表

## 十三、系统健康监控方案
- **监控指标**：CPU 使用率、内存使用率、磁盘空间、服务进程状态
- **告警阈值**：CPU > 80% 持续 5 分钟、内存 > 85%、磁盘 > 90%
- **自动处理**：
  - 内存持续增长：检测内存趋势，超过阈值时优雅重启 FastAPI（先返回维护页面，再重启）
  - CPU 持续高：自动重启爬虫脚本（如正在运行）
  - 磁盘超限：清理旧日志，备份并压缩
  - 服务无响应：自动重启 Docker 容器
- **定时重置**：仅在检测到内存持续增长趋势时触发重启（非固定时间）
- **日志记录**：所有监控事件记录到 system_log 表，后台可查看

## 十四、安全加固
- **后台管理认证**：
  - 用户名 + 密码 + 二次验证口令（TOTP 或固定二次口令）
  - 会话超时（15 分钟无活动自动登出）
  - 登录失败限流（5 次失败后锁定 30 分钟）
- **Nginx**：限流、防 CC、隐藏版本信息
- **SSL**：Let's Encrypt 自动续期
- **数据库**：定期备份到 S3，加密存储
- **爬虫**：请求频率控制（每站点每分钟不超过 10 次）、User-Agent 轮换、Cloudflare 绕过（如需要）

## 十五、爬虫反爬策略
- **请求频率**：每站点每分钟不超过 10 次请求，随机延迟 5-15 秒
- **User-Agent**：轮换 5-10 个常见浏览器 UA
- **请求头**：模拟完整浏览器请求头（Accept, Referer, Accept-Language）
- **失败处理**：
  - HTTP 429/503：指数退避重试（1min, 5min, 15min）
  - Cloudflare 挑战：记录失败，跳过该站点，通知管理员
  - 连续 3 天失败：暂停该站点爬虫，发送告警
- **数据验证**：抓取后验证 HTML 结构，失败则丢弃

## 十六、备份与恢复
- **自动备份**：每日 02:00 备份 SQLite + 静态文件到 S3
- **保留策略**：保留最近 30 天备份
- **恢复脚本**：`restore.py` 支持按时间点恢复
- **失败处理**：
  - 备份前验证上次备份是否成功
  - 失败时重试 3 次（间隔 5 分钟）
  - 连续失败 3 天：发送告警通知
- **监控**：备份成功/失败记录到 system_log 表
